<!-- @question = Question.find(params[:id]) passed to form -->
<!-- @question_options = @question.question_options passed to form -->
<!-- @submission = Submission.new(user_id: current_user.id) passed to form -->

<%= form_for @submission, url: {action: "create"} do |f| %>

  <%= @question.content %><br />

  <% @question_options.each do |que_opt| %>
     <%= f.radio_button_tag(:que_opt, que_opt.option.value) %><br />
  <% end %>

  <%= f.submit "Submit" %>

<% end %>
