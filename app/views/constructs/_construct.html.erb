<% construct.subject.each do |subject| %>

  <%= render subject %>

<% end %>

<% if construct.has? :references %>
  
  <% construct.references.each do |reference| %>

    <%= render reference %>
    
  <% end %>

<% end %>